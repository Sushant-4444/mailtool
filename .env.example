# ==============================================
# MAILTOOL - Email Configuration
# ==============================================
# Copy this file to .env and fill in your details
# NEVER commit .env to version control!

# ==============================================
# EMAIL SETTINGS (Required)
# ==============================================
# Your Gmail or SMTP email address
EMAIL_USER=your-email@gmail.com

# For Gmail: Use "App Password" (requires 2FA enabled)
# 1. Go to: https://myaccount.google.com/apppasswords
# 2. Generate new app password
# 3. Use that 16-digit password below
EMAIL_PASS=your-app-password-here

# For Gmail, use these settings:
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587

# For Outlook/Hotmail:
# EMAIL_HOST=smtp-mail.outlook.com
# EMAIL_PORT=587

# For other SMTP providers, check their documentation

# ==============================================
# SERVER SETTINGS
# ==============================================
PORT=5000
NODE_ENV=production

# ==============================================
# REDIS SETTINGS (Job Queue)
# ==============================================
# Redis connection (default works with Docker)
REDIS_HOST=redis
REDIS_PORT=6379

# ==============================================
# PERFORMANCE SETTINGS (Email Sending)
# ==============================================
# How many emails to send in parallel per batch
# Gmail Personal: 5-10 (SAFE - avoids rate limits)
# Gmail Workspace: 10-20
# Dedicated SMTP: 50-100
EMAIL_BATCH_SIZE=8

# Delay between batches in milliseconds
# Gmail Personal: 500-1000ms (SAFE - ~15-20 emails/min)
# Gmail Workspace: 200-500ms
# Dedicated SMTP: 50-100ms
EMAIL_BATCH_DELAY=600

# How many campaigns can process simultaneously
# Gmail Personal: 1-2 recommended to stay under daily 500 limit
# Dedicated SMTP: 3-5
WORKER_CONCURRENCY=2

# Worker rate limiting (jobs per duration)
WORKER_MAX_JOBS=3
WORKER_DURATION=1000
